local options = {
  ensure_installed = { "lua", "vim", "c", "markdown", "markdown_inline", "rust" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
